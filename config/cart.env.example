# ============================================================
# /etc/emart/cart.env  —  Cart Service environment
#
# Copy: sudo cp config/cart.env.example /etc/emart/cart.env
# Fill in values, then:
#   sudo chmod 640 /etc/emart/cart.env
#   sudo chown root:emart /etc/emart/cart.env
# ============================================================

APP_ENV=prod
APP_NAME=emart-cart-service
APP_VERSION=1.0.0
GIN_MODE=release

# HTTP port (Cart Service)
SERVER_PORT=8081

# MongoDB (cart database)
MONGO_URI=mongodb://emart_user:CHANGE_ME@localhost:27017
MONGO_DATABASE=cart

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD
REDIS_DB=0
REDIS_CART_TTL=168h        # 7 days

# !! MUST match JWT_SECRET in login.env exactly !!
JWT_SECRET=CHANGE_ME_MIN_64_CHARS_BASE64_ENCODED_SECRET

# Background sync: Redis → MongoDB
SYNC_INTERVAL=30s
SYNC_BATCH_SIZE=100
