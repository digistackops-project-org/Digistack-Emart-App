# ============================================================
# /etc/emart/login.env  —  Login Service environment
#
# Copy: sudo cp config/login.env.example /etc/emart/login.env
# Fill in values, then:
#   sudo chmod 640 /etc/emart/login.env
#   sudo chown root:emart /etc/emart/login.env
# ============================================================

# Spring active profile: dev | prod
SPRING_PROFILE=prod

# MongoDB URI  (userdb database, login service)
MONGO_URI=mongodb://emart_user:CHANGE_ME@localhost:27017/userdb?authSource=userdb

# !! MUST match JWT_SECRET in cart.env exactly !!
# Generate: openssl rand -base64 64 | tr -d '\n'
JWT_SECRET=CHANGE_ME_MIN_64_CHARS_BASE64_ENCODED_SECRET

# Token lifetime:  3600000=1h | 86400000=24h | 604800000=7d
JWT_EXPIRATION_MS=3600000

# HTTP port (Login Service)
SERVER_PORT=8080
